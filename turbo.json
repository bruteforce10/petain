{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**", ".next/**", "dist/**"]
    },
    "compile": {
      "dependsOn": ["^build"]
    },
    "lint": {}
  }
}
